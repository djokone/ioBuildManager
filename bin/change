#! /usr/bin/env node

var io = require('e:/sites/myModules/NPM/scripts/Iorizon-cli/bin/lib/Io') // before creating a npm package

change = new io()
console.log(change.parsedArgv)
if (change.hasCurrentModules && change.module.isLoaded) {
  change.current.modules.each((k, v) => {
    // if (change.global.Alias((v.url) === thi) {}
  })
}

// console.log(change.current.content.modules)
// console.log(change.currentPath)